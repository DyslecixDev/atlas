# Enforce the Node version range declared in package.json engines.
engine-strict=true

# corepack already provides the pinned pnpm; don't let pnpm also self-switch to
# its native build (the two managers conflict and the native install is flaky).
manage-package-manager-versions=false
